{
  "name": "archivox",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @archivox/web",
    "build": "npm run build --workspace @archivox/web",
    "lint": "npm run lint --workspaces",
    "test": "echo \"(no tests yet)\""
  },
  "dependencies": {
    "pg": "^8.20.0"
  }
}
